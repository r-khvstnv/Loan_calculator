<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rssll971.loancalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_house_launcher_v2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_house_launcher_v2_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoanCalc"
        >
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateAlwaysVisible"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Sample AdMob app ID: ca-app-pub-3940256099942544~3347511713 -->

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/ad_id" />
    </application>
</manifest>